apiVersion: v1
kind: Secret
metadata:
  name: mongodb-secret
type: Opaque
data:
  MONGO_INITDB_ROOT_USERNAME: YWRtaW4=  # Base64 encoded value of "admin"
  MONGO_INITDB_ROOT_PASSWORD: cGFzc3dvcmQ=  # Base64 encoded value of "password"